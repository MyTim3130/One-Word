'use client';
import React, { useEffect, useState } from 'react';

const Page = () => {


  return (
    <div>
      <button>{connected ? 'Connected' : 'Not Connected'}</button>
    </div>
  );
};

export default Page;
